import { EchartsDirTestDirective } from './echarts-dir-test.directive';

describe('EchartsDirTestDirective', () => {
  it('should create an instance', () => {
    const directive = new EchartsDirTestDirective();
    expect(directive).toBeTruthy();
  });
});
